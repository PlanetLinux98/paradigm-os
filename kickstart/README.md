# kickstart/

Fedora kickstart (`.ks`) files defining ParadigmOS's package set, repositories
(including RPM Fusion), partitioning defaults (Btrfs + snapshots, optional LUKS),
and post-install branding hooks (logo, `/etc/os-release`, wallpaper, theme,
Anaconda welcome banner).

Nothing here yet — this is the next concrete build step after the branding
assets and package list are finalized.
